# .dotfiles

## Repository-specific guidance

- Treat changes here as changes to user-level tooling: preserve compatibility with both Claude Code and Codex unless a file is explicitly client-specific.
- Work on a `feature/*` branch. Never commit directly to `main`.
- Run the relevant configuration validators before committing.
